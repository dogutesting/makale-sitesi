/* * @type {import('next').NextConfig}
const nextConfig = {
  // swcMinify: false,
  transpilePackages: ['crypto-js'],
} */

module.exports = {
  /* transpilePackage: ['crypto-js'], */
  /* api: {
    bodyParser: {
      sizeLimit: '10kb'
    }
  } */
}