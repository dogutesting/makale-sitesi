-> ana sayfadanın urls params'larının çalışması ve geri tuşunun çalışmasının düzenlenmesi
	-V-> herhalde çözüldü bende tam anlayamadım

-> makale sayfasındaki önerileri ayarla
 	-V-> sanırım oldu gibi
	

-> ana sayfa mobil img'ler doğru gözükmüyor düzenle
	-V-> doğru gibi

-> CategoryBox doğru çalışmıyor
	(400 px altında (css media ile alakasız bir noktada) doğru çalışıyor, ama yukarılarda çalışmıyor | ctrl + shift + r gerekli)
	V

-> sayfa içi h1 yanlarına paylaş ikonu ekle
	-> V

-> details içindeki ul ve li'ler hatalı gözüküyor
		
-> addClick'deki / olayı kaldırılsın, performans için V

-> geo location : //https://www.iplocation.net/ip-lookup

-> makale sayfalarına "react-infinite-scroll-component" ekle (alakalı makale devam etsin ve yeni alakalı öneriler sunsun)
	-> react-infinite-scroll içerisindeki hatalar
		0-) her sayfanın getStaticProps özelliği alta çekilmeli, import getStaticProps'u çalıştırmıyor -> V
		1-) paylaş butonu doğru çalışmıyor -> V
		
		2-) sayfa yenilendiğinde en son bakılan yerden yenilenecek
			-> observer veya h1 ve end etiketleri takip edilebilir
				2.1 -> adblock varken observer çalışmıyor
					Olası çözümler: observer yerine farklı bir yöntem denemek

					2.2 ->
							- github'dan eski bir commit indirilerek sayfa düzenine ve text'lere bakılacak
								bir hata mı var eksik mi var diye

							- dinamik olarak yüklenen info'lar hydration hatası veriyor
							
							- waypoint olayı çözülecek

		2.1-) v getAllArticlesForUser() fonksiyonu doğru çalışıyor mu diye kontrol edilecek 
		3-) bakılan sayfalar database'e eklenmiyor

		4-) v resimlerin border-radius'ları doğru çalışmıyor
		5-) v problem görülebilir mi bilmiyorum ama enonlar ve nightModeButonu her page'de render ediyor


V? -> mozilla hatası | ilginç bir şekilde hata düzelmiş

V -> users ekleme çalışmıyor | artık doğru çalışıyor

V -> Custom_Waypoint içerisinde bazı kodlar daha yazılacak 

x iptal edildi;sebeb: en çok uğradıkları noktaları çekiyorum
-> infinitescroll ve othercontents içerisinde oluşturulan sayfalar düzenlenmeli (yeni type'lar eklendi: top-waypoint, bot-waypoint- middleware)

V -> infinitescroll ile yüklenen componentlerde pre-load kaldırılabilir

V -> oyuncular [array] olarak gönderiliyor ama kunye içerisinde garip yazılıyor

v -> gece modunda ana sayfa ve yazılarında linklerin gözükme rengini ayarla

v -> fare geri tuşuna basılınca kod doğru çalışmıyor, window.pushState

v -> webSupport state'i sayfa yenilendikten sonra kendisini tekrar başlatıyor. Performans kaybı olarak değerlendirilebilir, local storage'de saklarsak tekrar tekrar hesap yapmamış oluruz.

V-> İSTEK LİMİTLERİ DÜZENLENECEK, SAYFA İLK YÜKLENDİĞİNDE GUİ VE GUİL İSTEĞİ YAPMASINA GEREK YOK ALTA GELİNCE GUİ İSTEĞİ YAPABİLİR VE EĞER HIZLI DEĞİLSE GUİL İSTEĞİ YAPABİLİR

V -> getArticlesForUser, otherContents her seferinde istek atması düzenlenmeli !!BURADA KALDIN: uuid oluşturmak için istek sayısı veya diğer yollar için istek sayısı ayarlanmalı

V -> getAllArticlesForUser her seferinde bütün tabloyu taraması engellenmeli 
	click-server'deki limitin benzeri burayada eklenmeli
	eğer zaman aşımı uygulanıyor ise infinite-scroll deactive olacak.


V? default olarak 4mb veri yollama limiti varmış next.js'in. Eğer kafama takılır ise nginx üzerinden kontrol ederim
-> hem click-server'a hemde next.js server'ına post veri yollama limiti eklenmeli 1 gb veri yollayarak sistemi yormamalı.

-> SERVER.JS dosyasındaki yollar userKey içerisine geri eklenecek.

-> getArticle sayfasına istek atma limiti eklenecek





-> sayfayı kullanarak bazı bilgileri paylaşmayı kabul ettiklerine dair bildirim kutusu gönder,
okudum'a basarsa kapat ve bir daha gösterme. Okudum'a bastıktan sonra tanımlamalara izin ver(city, id).

-> google reklamı eklenmesi gerekiyor

-> 10 makale daha yaz.

-> server'a uygulamayı yüklerken nginx kurulması gerekiyor, böylelikle
	sık sık talep edilen içerikler nginx tarafından önbelleğe alınır ve
		performans artışı sağlar. Aynı zamanda ön alan adı eklemeyi de mümkün
			kılar. market.enonlar.com | test.enonlar.com | ustalar.enonlar.com

-> console.log'ları yayınlamadan önce kaldır. Özellikle userKey gibi dosyalarda.
-> olmayan linklere gidildiğinde url'deki anahtar kelimeler ile mysql'deki anahtar kelimelerden eşleşen şeyler sunulabilir
-> kullanılmayan npm i kütüphanelerini bul ve sil
-> Lighthouse'da hata var mı diye kontrol (dosya boyutlarını küçültmek için minify (bundle)
	 veya https://github.com/johnagan/clean-webpack-plugin)
-> google search console'da hata var mı diye kontrol et
-> günde minimum 1 makale yaz ve diğer proje üzerinde çalış

Belki:
	-> fuse kütüphanesi ile arama barı eklenebilinir